import React from 'react';
import Spinner from '../../Shared/Spinner/Spinner';

const Dashboard = () => {
    return (
        <div>
            <Spinner></Spinner>
        </div>
    );
};

export default Dashboard;